Use the `simplify` skill to review the code changes made in this session.

Look specifically for:
- Opportunities to reuse existing utilities in `src/lib/` or `src/components/`
- Violations of project conventions (named function declarations, no arrow functions at top-level, `interface` over `type`, exact dep versions)
- Unnecessary complexity or abstractions for one-off operations
- Missing `server-only` imports on any new files under `src/lib/`
